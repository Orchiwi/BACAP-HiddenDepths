# bacaphd_sy is the current layer of the y walk, so it doubles as an altitude.
# Keep the LOWEST powered Bell.
scoreboard players add @s bacaphd_bells 1
scoreboard players operation @s bacaphd_ct_by < @s bacaphd_sy
