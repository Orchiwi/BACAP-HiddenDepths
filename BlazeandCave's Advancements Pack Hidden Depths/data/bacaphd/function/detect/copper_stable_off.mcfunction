# B9 Copper Stable. Runs as a player who was holding an anchor but was not on a
# copper-armoured horse this sample. Drop both flags so the next mount re-anchors.
tag @s remove bacaphd.cs_off
tag @s remove bacaphd.cstable
